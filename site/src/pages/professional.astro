---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import ThreeBackground from '../components/ThreeBackground.astro';
import Navigation from '../components/Navigation.astro';
import ProjectCard from '../components/ProjectCard.astro';

const allProfessional = await getCollection('professional');
const featuredProfessional = allProfessional.filter(p => p.data.featured);
const otherProfessional = allProfessional.filter(p => !p.data.featured);
---

<BaseLayout
  title="Professional - ronsween"
  description="Industrial-grade implementations and enterprise experience"
>
  <Navigation />

  <main class="projects-page">
    <div class="container">
      <header class="projects-header">
        <h1 class="page-title">Professional</h1>
        <p class="page-subtitle">
          Industrial-Grade Implementation
        </p>
      </header>

      {featuredProfessional.length > 0 && (
        <section class="featured-section">
          <div class="projects-grid featured-grid">
            {featuredProfessional.map((project) => (
              <ProjectCard
                title={project.data.title}
                description={project.data.description}
                image={project.data.image}
                link={project.data.link}
                github={project.data.github}
                tags={project.data.tags}
                featured={true}
              />
            ))}
          </div>
        </section>
      )}

      {otherProfessional.length > 0 && (
        <section class="other-projects">
          <div class="projects-grid">
            {otherProfessional.map((project) => (
              <ProjectCard
                title={project.data.title}
                description={project.data.description}
                image={project.data.image}
                link={project.data.link}
                github={project.data.github}
                tags={project.data.tags}
              />
            ))}
          </div>
        </section>
      )}
    </div>
  </main>
</BaseLayout>

<style>
  .projects-page {
    min-height: 100vh;
    padding: calc(var(--nav-height) + 2rem) 2rem 5rem;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .projects-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-title {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 8vw, 4rem);
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .page-subtitle {
    font-size: 1.1rem;
    color: var(--color-text-dim);
    max-width: 500px;
    margin: 0 auto;
  }

  .featured-section,
  .other-projects {
    margin-bottom: 4rem;
  }

  .section-title {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    color: var(--color-primary);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
  }

  .featured-grid {
    grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
  }

  @media (max-width: 768px) {
    .projects-page {
      padding-top: calc(var(--nav-height) + 1rem);
    }

    .projects-grid,
    .featured-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
